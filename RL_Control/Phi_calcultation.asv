function phi = Phi_calcultation(state, mu, sigma)
    x = state(1,1);
    v = state(2,1);
    
    phi = zeros(length(mu),1);
    % For each centrum
    for i = 1:length(mu)
       c_x = mu(i,1);
       c_v = mu(i,2);
       sigma_x = 
       
       coeff_1 = (x - c_x)^2 / (2 * sigma^2)
       coeff_2 = (y - c_y)
       phi(i,:) = exp()
    end
    
end

